# Service account for virt-launcher pods that need rawio capabilities.
# The pod must run under this SA (via a serviceAccount volume in the VM spec)
# so the validating webhook can verify it has the required SCC permissions.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rawio-vm
  namespace: my-namespace
