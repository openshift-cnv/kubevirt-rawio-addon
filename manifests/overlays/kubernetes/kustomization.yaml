apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - cert-manager.yaml

patches:
  - target:
      kind: MutatingWebhookConfiguration
      name: rawio-webhook
    patch: |
      - op: add
        path: /metadata/annotations
        value:
          cert-manager.io/inject-ca-from: kubevirt-rawio-addon/rawio-webhook-tls
